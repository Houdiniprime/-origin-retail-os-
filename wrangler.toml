# Configuration Cloudflare Pages - Origin Retail OS
# Documentation: https://developers.cloudflare.com/pages/

name = "origin-retail-os"
pages_build_output_dir = "."

# Le binding KV est configuré dans le dashboard Cloudflare Pages :
# Settings → Functions → KV namespace bindings → Ajouter ERP_STATE
# (ne pas mettre dans wrangler.toml pour Pages)
